<Window x:Class="ContentControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContentControl"
        mc:Ignorable="d"
        Title="ContentControl类" Height="450" Width="800">
    <Grid>
        <ContentControl FontSize="40" Foreground="Red">
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <Border CornerRadius="10" Padding="15" Background="Red" BorderBrush="Red" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="{Binding}" Foreground="White"/>
                    </Border>
                </DataTemplate>
            </ContentControl.ContentTemplate>               
           WPF学习教程
        </ContentControl>
    </Grid>
</Window>
