<Window x:Class="DataBoard.Windows.AddLineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataBoard.Windows"
        mc:Ignorable="d" WindowStyle="None"
        Background="#29282D"
        WindowStartupLocation="CenterScreen" 
        DataContext="{Binding Source={StaticResource Locator},Path=AddLineWindow}"
        Title="添加生产线" Height="200" Width="400">
    <Grid>
        <Grid Margin="30 50 30 40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Height="35" Text="{Binding Line .Name,Mode=TwoWay,
        UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="1" Margin="15 15 15 15" FontSize="16" Content="添加" Tag="&#xf055;" HorizontalAlignment="Right" Command="{Binding AddLineCommand}" Style="{StaticResource AddButtonStyle}"/>
        </Grid>


    </Grid>
</Window>
